import { FileEntry } from '../file-entry';
interface Props {
    file: FileEntry;
    className?: string;
    iconClassName?: string;
    showImage?: boolean;
}
export declare function FileThumbnail({ file, className, iconClassName, showImage, }: Props): import("react/jsx-runtime").JSX.Element;
export {};
