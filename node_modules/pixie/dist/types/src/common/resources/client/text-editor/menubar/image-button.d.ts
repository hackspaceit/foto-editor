import { MenubarButtonProps } from './menubar-button-props';
interface Props extends MenubarButtonProps {
    diskPrefix?: string;
}
export declare function ImageButton({ editor, size, diskPrefix }: Props): import("react/jsx-runtime").JSX.Element;
export {};
