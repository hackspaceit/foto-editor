type Props = {
    defaultValue?: string;
    onChange?: (e: string) => void;
    colorPresets?: string[];
    showInput?: boolean;
};
export declare function ColorPicker({ defaultValue, onChange, colorPresets, showInput, }: Props): import("react/jsx-runtime").JSX.Element;
export {};
