import { ReactNode } from 'react';
interface Props {
    children: ReactNode;
    modifier?: boolean;
    separator?: string;
}
export declare function Keyboard({ children, modifier, separator }: Props): import("react/jsx-runtime").JSX.Element;
export {};
